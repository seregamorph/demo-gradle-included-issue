plugins {
    id 'java-gradle-plugin'
}

gradlePlugin {
    plugins {
        reproducibleBuild {
            id = 'reproducible-build'
            implementationClass = 'org.example.plugin.ReproducibleBuildProjectPlugin'
        }
    }
}
